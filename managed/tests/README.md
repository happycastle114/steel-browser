# Managed test fixtures

Cross-workspace contract fixtures live here. Task 3 adds the locked upstream REST and WebSocket
corpus under `upstream/<upstreamSha>/`; later tasks consume that committed corpus from fresh clones.

Evidence and real credentials never belong in this directory.
