# Coolify deployment boundary

This directory is reserved for generated, digest-pinned managed blue/green deployment bundles.
Task 24 owns the first deployable Compose output. The Task 1 scaffold intentionally contains no
image tag, public worker port, Docker socket, credential, or runnable deployment.
